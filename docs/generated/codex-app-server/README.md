# Generated Codex App Server schema

This artifact was generated from the installed Codex CLI used for HealthCoach, not copied from an older protocol example.

```sh
codex app-server generate-json-schema --out docs/generated/codex-app-server
```

The committed aggregate schema is `codex_app_server_protocol.schemas.json`. The recorded source is `codex-cli 0.154.0-alpha.6.2`; the host uses the installed binary at runtime and fails closed when the effective HealthCoach MCP catalog cannot be verified.
