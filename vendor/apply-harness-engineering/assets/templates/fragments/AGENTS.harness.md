## Agent harness routing

- Start at the task-oriented [agent harness index](docs/agent-harness/index.md); it routes every operational authority through real Markdown links.
- Treat [`docs/agent-harness/config.json`](docs/agent-harness/config.json) only as the declared authority map, never as a dynamic redirect.
- Use the declared planning policy for cross-cutting, risky, multi-hour, or context-loss-sensitive work.
- Keep active ExecPlans current; move them only after the requested outcome and applicable checks pass.
- Use the linked verification matrix to select checks and the linked output contract to report evidence literally.
- Record recurring failures in the appropriate canonical document, test, enforceable rule, or debt tracker.

<!-- Merge and tailor this fragment; never replace an existing AGENTS.md blindly. -->
